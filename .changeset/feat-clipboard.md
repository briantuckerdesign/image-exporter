---
"image-exporter": minor
---

Add `copyImageToClipboard(image)` to copy a single captured image to the system clipboard (also exposed as `window.imageExporterCopyToClipboard`). Use `format: "png"` for best browser compatibility.
